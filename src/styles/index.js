import { StyleSheet } from 'react-native'
import APP_COLORS from './colors'

const style = StyleSheet.create({
  mainContainer: {
    flex: 1,
    backgroundColor: '#FFFFFF',//APP_COLORS.lightPrimaryColor,
  }
})

export default style
